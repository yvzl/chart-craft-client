import type {MapOption} from "@/types";

export const verticalAlign: MapOption = {
    name: "垂直对齐方式",
    type: "map",
    data: {
        top: "顶部",
        middle: "居中",
        bottom: "底部"
    }
}