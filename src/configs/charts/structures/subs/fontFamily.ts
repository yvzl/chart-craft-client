import type {MapOption} from "@/types";

export const fontFamily: MapOption = {
    name: "字体系列",
    type: "map",
    data: {
        "sans-serif": "sans-serif",
        serif: "serif",
        monospace: "monospace",
        Arial: "Arial",
        "Courier New": "Courier New",
    }
}