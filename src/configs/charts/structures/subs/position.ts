import type {MapOption} from "@/types";

export const position: MapOption = {
    name: "位置",
    type: "map",
    data: {
        top: "顶部",
        left: "左侧",
        right: "右侧",
        bottom: "底部",
        inside: "内侧",
    }
}